# FullPageDashboard repo & branch
[ -n "$PIJAM_FULLPAGEOS_DASHBOARD_CLIENT_REPO_SHIP" ] || FULLPAGEOS_DASHBOARD_REPO_SHIP=https://github.com/tailorvj/fullpagedashboard-client.git
[ -n "$PIJAM_FULLPAGEOS_DASHBOARD_CLIENT_REPO_BUILD" ] || FULLPAGEOS_DASHBOARD_REPO_BUILD=
[ -n "$PIJAM_FULLPAGEOS_DASHBOARD_CLIENT_REPO_BRANCH" ] || FULLPAGEOS_DASHBOARD_REPO_BRANCH=master
